# Optional Blockchain Adapter

Disabled by default.

The adapter may anchor hashes for selected traceability records. It must not:

- store raw telemetry or images on-chain;
- block harvest or customer workflows;
- be described as proof that source data was truthful;
- require the core domain to import Fabric-specific types.

Enable only through configuration and an approved implementation plan.
