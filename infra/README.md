# Infrastructure

Infrastructure is split by concern:

- `database/`
- `mqtt/`
- `media/`
- `blockchain/`
- `observability/`
- `docker/`

Do not add production complexity before the MVP flow is stable.
