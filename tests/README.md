# Cross-System Tests

- `e2e/`: critical user journeys
- `contract/`: shared contract validation
- `fixtures/`: deterministic non-sensitive fixtures
- `hardware/`: simulator and gateway compatibility tests

Do not duplicate module unit tests here.
