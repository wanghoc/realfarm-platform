# Scripts

Place repeatable developer scripts here.

Scripts must:

- be safe by default;
- support `--help`;
- avoid destructive behavior without confirmation;
- return non-zero exit codes on failure;
- never print secrets.
